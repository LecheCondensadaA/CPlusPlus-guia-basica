// hola soy un comentario, este es el Hola mundo en C++, esta todo explicado para entender como funciona
#include <iostream> // Incluye la libreria iostream, que contiene cout
using namespace std; // Esto es para no escribir todo el rato std:: cada vez que quieras usar algo de una libreria

int main(){ // Todo programa de C++ tiene un main
    cout << "Hola mundo!" << endl; // Cout escribe mientras que endl (endline) marca un salto de linea
    return 0; // No es obligacion, pero marca que el programa finalizo exitosamente
}
